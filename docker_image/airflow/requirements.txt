# --- Các gói mở rộng (Providers) của Airflow ---
apache-airflow-providers-apache-spark==4.1.3   # Để gửi lệnh tới Spark Master
apache-airflow-providers-amazon==8.10.0        # Để kết nối AWS S3 / MinIO

# --- Các thư viện Python phục vụ Job ---
pyspark==3.3.2
mlflow==2.8.0
pandas==1.5.3
polars==0.19.19
pymysql==1.1.0
minio==7.1.15
sqlalchemy>=1.4.28,<2.0
Flask-Session>=0.5.0
delta-spark==2.3.0
#pyarrow>=10.0.1
#deltalake>=0.15.0